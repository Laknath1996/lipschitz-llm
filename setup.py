from setuptools import setup, find_packages

setup(
    name='llm_work',
    version='0.1',
    packages=find_packages(),
    description='experiments involving LLMs',
    author='Ashwin De Silva',
)
